# MongoDB connection (default: mongodb://localhost:27017/airbnb_clone)
MONGODB_URI=mongodb://localhost:27017/airbnb_clone

# JWT secret: use a strong random value (min 16 chars) in production; required when NODE_ENV=production
JWT_SECRET=your_secret_key_change_in_production

# Optional: port (default 5000)
# PORT=5000
